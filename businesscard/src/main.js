
// import { createRoot } from 'react-dom/client';
import AppForCode from './AppForCode';


const root = createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AppForCode />
  </React.StrictMode>
);